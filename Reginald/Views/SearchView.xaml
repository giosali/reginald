<Window x:Name="searchWindow"
        x:Class="Reginald.Views.SearchView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Reginald.Views"
        mc:Ignorable="d"
        xmlns:sys="clr-namespace:System;assembly=mscorlib" 
        
        xmlns:cal="http://www.caliburnproject.org"
        xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
                  
        xmlns:fw="clr-namespace:SourceChord.FluentWPF;assembly=FluentWPF"
        fw:AcrylicWindow.Enabled="True"
        fw:AcrylicWindow.TintColor="{Binding Settings.SearchBackgroundColor}"
        fw:AcrylicWindow.TintOpacity="0.8"
        fw:AcrylicWindow.NoiseOpacity="0"
        fw:AcrylicWindow.AcrylicWindowStyle="None"
                  
        Title="Search"
        Height="65" MinHeight="65"
        Width="600"
        FontFamily="Segoe UI"
        ResizeMode="NoResize"
        SizeToContent="Height"
        BorderBrush="{Binding Settings.SearchViewBorderBrush}"
        
        WindowStartupLocation="Manual"
        WindowStyle="None"
        Topmost="True"
        ShowInTaskbar="False">
    <Window.Resources>
        <ResourceDictionary>
            <!-- ItemTemplate for SearchResults -->
            <DataTemplate x:Key="SearchResultsItemTemplate">
                <Grid Width="570">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <Image Source="{Binding Icon}"
                           Width="30" Height="30"
                           Margin="0 0 12.5 0"
                           Grid.Column="0"/>
                    <StackPanel VerticalAlignment="Center"
                                Margin="0 0 0 5"
                                Grid.Column="1" Grid.ColumnSpan="2">
                        <TextBlock Text="{Binding Description}" TextTrimming="CharacterEllipsis"
                                   Background="Transparent" Foreground="{Binding ElementName=searchWindow, Path=DataContext.Settings.SearchDescriptionTextBrush}"
                                   FontSize="24" FontWeight="Light"
                                   Margin="0" Padding="0"/>
                        <TextBlock Text="{Binding Alt}"
                                   Background="Transparent" Foreground="{Binding ElementName=searchWindow, Path=DataContext.Settings.SearchAltTextBrush}"
                                   FontSize="12" FontWeight="Light"
                                   Margin="0" Padding="0"/>
                    </StackPanel>
                    <!--<TextBlock Text="{Binding Count}"
                               Background="Transparent" Foreground="Gray"
                               FontSize="12" FontWeight="Light"
                               VerticalAlignment="Center" HorizontalAlignment="Right"
                               Grid.Column="3"
                               Margin="5 0 0 0" Padding="0 0 30 0"/>-->
                </Grid>
            </DataTemplate>

            <!-- ControlTemplate for ListBoxItems-->
            <ControlTemplate x:Key="SearchResultsListBoxItems" TargetType="ListBoxItem">
                <Border x:Name="listBoxItemBorder"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            Padding="{TemplateBinding Padding}"
                            SnapsToDevicePixels="True">
                    <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                      SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"
                                      Content="{TemplateBinding ContentControl.Content}"
                                      ContentTemplate="{TemplateBinding ContentControl.ContentTemplate}"/>
                </Border>
                <ControlTemplate.Triggers>
                    <Trigger Property="IsMouseOver" Value="True">
                        <Setter TargetName="listBoxItemBorder"
                                Property="Background" Value="Transparent"/>
                    </Trigger>
                    <Trigger Property="IsSelected" Value="True">
                        <Setter TargetName="listBoxItemBorder"
                                Property="Background" Value="#99ff8d00"/>
                    </Trigger>
                </ControlTemplate.Triggers>
            </ControlTemplate>

            <!-- ItemTemplate for SpecialSearchResult -->
            <DataTemplate x:Key="SpecialSearchResultItemTemplate">
                <DataTemplate.Resources>
                    <Style TargetType="TextBlock">
                        <Setter Property="VerticalAlignment" Value="Center"/>
                    </Style>
                    <Style x:Key="SubTextBlock" TargetType="TextBlock">
                        <Setter Property="Foreground" Value="DarkGray"/>
                        <Setter Property="FontWeight" Value="Light"/>
                        <Setter Property="FontSize" Value="16"/>
                    </Style>
                </DataTemplate.Resources>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <!-- Row 0 -->
                    <Border Grid.Row="0"
                            Grid.Column="0" Grid.ColumnSpan="{x:Static sys:Int32.MaxValue}"
                            BorderBrush="#B7ADAC"
                            BorderThickness="0.25"
                            Margin="0 0 0 10"/>
                    <!-- Row 1 -->
                    <Image Source="{Binding Icon}"
                           Grid.Row="1"
                           Grid.Column="0"
                           Width="75" Height="75"
                           HorizontalAlignment="Left"
                           Margin="20 0 0 0"/>
                    <TextBlock Text="{Binding Name}"
                               Grid.Row="1"
                               Grid.Column="1"
                               FontSize="32"
                               TextAlignment="Left"
                               HorizontalAlignment="Left"
                               Margin="-75 0 0 0"/>
                    <TextBlock Text="{Binding Description}"
                               Grid.Row="1"
                               Grid.Column="4"
                               FontSize="18" FontWeight="Light"
                               HorizontalAlignment="Right"
                               Foreground="DarkGray"
                               Margin="0 0 20 0"/>
                    <!-- Row 2 -->
                    <Border Grid.Row="2"
                            Grid.Column="0" Grid.ColumnSpan="{x:Static sys:Int32.MaxValue}"
                            BorderBrush="#B7ADAC"
                            BorderThickness="0.25"
                            Margin="20 20 20 10"/>
                    <!-- Row 3 -->
                    <TextBlock Text="{Binding Major}"
                               Grid.Row="3"
                               Grid.Column="0" Grid.ColumnSpan="1"
                               FontSize="40" FontWeight="Bold"
                               Margin="20 0 0 0"/>
                    <TextBlock Text="{Binding MajorTwo}"
                               Grid.Row="3"
                               Grid.Column="4"
                               FontSize="24" FontWeight="Light"
                               HorizontalAlignment="Right"
                               Margin="0 0 20 0"/>
                    <!-- Row 4 -->
                    <TextBlock Text="{Binding Minor}"
                               Grid.Row="4"
                               Grid.Column="0" Grid.ColumnSpan="1"
                               FontSize="32" FontWeight="Light"
                               Foreground="{Binding MinorTextBrush}"
                               Margin="20 0 0 0"/>
                    <TextBlock Text="{Binding MinorTwo}"
                               Grid.Row="4"
                               Grid.Column="4"
                               FontSize="18" FontWeight="Light"
                               Foreground="DarkGray"
                               HorizontalAlignment="Right"
                               Margin="0 0 20 0"/>
                    <!-- Row 5 -->
                    <Border Grid.Row="5"
                            Grid.Column="0" Grid.ColumnSpan="{x:Static sys:Int32.MaxValue}"
                            Margin="0 10"/>
                    <!-- Row 6 -->
                    <TextBlock Text="{Binding SubOne}"
                               Style="{StaticResource SubTextBlock}"
                               Grid.Row="6"
                               Grid.Column="0"
                               Margin="20 0 0 0"/>
                    <!-- Row 7 -->
                    <TextBlock Text="{Binding SubTwo}"
                               Style="{StaticResource SubTextBlock}"
                               Grid.Row="7"
                               Grid.Column="0"
                               Margin="20 0 0 0"/>
                    <!-- Row 8 -->
                    <Border Grid.Row="8"
                            Grid.Column="0" Grid.ColumnSpan="{x:Static sys:Int32.MaxValue}"
                            Margin="0 10 0 0"/>
                </Grid>
            </DataTemplate>
        </ResourceDictionary>
    </Window.Resources>

    <Grid>
        <StackPanel>
            <TextBox x:Name="UserInput"
                     Background="Transparent" Foreground="{Binding Settings.SearchInputTextBrush}"
                     CaretBrush="{Binding Settings.SearchInputCaretBrush}"
                     BorderThickness="0"
                     FontSize="32" FontWeight="Light"
                     Padding="10 0"
                     VerticalContentAlignment="Center"
                     Height="{Binding ElementName=searchWindow, Path=MinHeight}">
                <i:Interaction.Triggers>
                    <i:EventTrigger EventName="TextChanged">
                        <cal:ActionMessage MethodName="UserInput_TextChangedAsync">
                            <cal:Parameter Value="{Binding ElementName=SearchResults}"/>
                            <cal:Parameter Value="$eventargs"/>
                        </cal:ActionMessage>
                    </i:EventTrigger>
                    <i:EventTrigger EventName="PreviewKeyDown">
                        <cal:ActionMessage MethodName="UserInput_PreviewKeyDown">
                            <cal:Parameter Value="{Binding ElementName=UserInput}"/>
                            <cal:Parameter Value="$eventargs"/>
                        </cal:ActionMessage>
                    </i:EventTrigger>
                </i:Interaction.Triggers>
            </TextBox>

            <ListBox x:Name="SearchResults"
                     ItemTemplate="{StaticResource SearchResultsItemTemplate}"
                     SelectedItem="{Binding Path=SelectedSearchResult, Mode=TwoWay}"
                     Background="Transparent"
                     MaxHeight="330"
                     ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                     ScrollViewer.VerticalScrollBarVisibility="Auto"
                     ScrollViewer.CanContentScroll="False"
                     BorderThickness="0"
                     Visibility="{Binding Path=IsVisible}">
                <i:Interaction.Triggers>
                    <i:EventTrigger EventName="PreviewMouseLeftButtonDown">
                        <cal:ActionMessage MethodName="SearchResults_PreviewMouseLeftButtonDown">
                            <cal:Parameter Value="{Binding ElementName=SearchResults}"/>
                            <cal:Parameter Value="$eventargs"/>
                        </cal:ActionMessage>
                    </i:EventTrigger>
                    <i:EventTrigger EventName="MouseLeave">
                        <cal:ActionMessage MethodName="SearchResults_MouseLeave">
                            <cal:Parameter Value="{Binding ElementName=SearchResults}"/>
                            <cal:Parameter Value="$eventargs"/>
                        </cal:ActionMessage>
                    </i:EventTrigger>
                    <i:EventTrigger EventName="SelectionChanged">
                        <cal:ActionMessage MethodName="SearchResults_SelectionChanged">
                            <cal:Parameter Value="{Binding ElementName=SearchResults}"/>
                            <cal:Parameter Value="$eventargs"/>
                        </cal:ActionMessage>
                    </i:EventTrigger>
                </i:Interaction.Triggers>
                <ListBox.ItemContainerStyle>
                    <Style TargetType="ListBoxItem" BasedOn="{StaticResource {x:Type ListBoxItem}}">
                        <Style.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="IsSelected" Value="True"/>
                            </Trigger>
                        </Style.Triggers>
                        <Setter Property="Padding" Value="10 0"/>
                        <Setter Property="Height" Value="55"/>
                        <Setter Property="Template" Value="{StaticResource SearchResultsListBoxItems}"/>
                    </Style>
                </ListBox.ItemContainerStyle>
            </ListBox>

            <ItemsControl x:Name="SpecialSearchResults"
                          ItemTemplate="{StaticResource SpecialSearchResultItemTemplate}"
                          Background="Transparent"
                          MaxHeight="330"
                          MaxWidth="600"
                          BorderThickness="0"
                          Visibility="Visible">
            </ItemsControl>
        </StackPanel>
    </Grid>
</Window>